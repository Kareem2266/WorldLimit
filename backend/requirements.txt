# FastAPI: the framework that handles HTTP requests and routing
fastapi==0.111.0
# uvicorn: the server that actually runs FastAPI and listens for connections
# [standard] installs optional extras like faster JSON handling
uvicorn[standard]==0.29.0

# asyncpg: speaks PostgreSQL's protocol directly — we write raw SQL
# and asyncpg executes it. No ORM in the middle.
asyncpg==0.29.0
# alembic: handles database migrations (version control for your schema)
alembic==1.13.1
# psycopg2-binary: synchronous postgres driver used ONLY by Alembic.
# Alembic was built for synchronous drivers. Our app uses asyncpg,
# but migrations need this to actually connect and run the SQL.
psycopg2-binary==2.9.9

# celery: the task queue library that powers our background worker
celery==5.3.6
# redis: the Python client for talking to the Redis server
redis==5.0.4

# pydantic: validates data shapes — if a request is missing a field,
# pydantic catches it before your code ever runs
pydantic==2.7.1
# pydantic-settings: extends pydantic to read config from .env files
pydantic-settings==2.2.1

# python-dotenv: loads .env files into environment variables
python-dotenv==1.0.1
# httpx: async HTTP client for making requests to external APIs
httpx==0.27.0
# python-multipart: required for file upload support in FastAPI
python-multipart==0.0.9
# numpy: fast numerical computation — used in terrain generation
numpy==1.26.4
# rasterio: reads and writes geospatial raster files (GeoTIFF, HGT, etc.)
rasterio==1.4.3
# scikit-learn: machine learning toolkit — we use it for k-means clustering
scikit-learn==1.4.2
# pandas: tabular data — used to build and save the feature CSV
pandas==2.2.2

# torch: PyTorch — defines + trains the text→terrain MLP and runs inference.
# We pull from the CPU-only wheel index to avoid the ~2 GB CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
# sentence-transformers: turns a string into a 384-dim semantic vector
# (pre-trained model: all-MiniLM-L6-v2). Pulls in transformers + tokenizers.
sentence-transformers==2.7.0

# noise: Ken Perlin's reference implementation of Perlin/simplex noise in C.
# Used to synthesize procedural heightmaps from the predicted terrain params.
noise==1.2.2
# Pillow: saves heightmaps as 16-bit grayscale PNGs for Phase 5 / rendering.
Pillow==10.3.0
